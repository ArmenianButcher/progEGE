#include<iostream>
#include<vector>
using namespace std;



int main() {
	int number;
	cin >> number;
	if(((number/100)>=1&& (number/100) <= 9)&&(number%2!=0)) {
		cout << "YES" << endl;
	} else cout << "NO" << endl;
	return 0;
}
